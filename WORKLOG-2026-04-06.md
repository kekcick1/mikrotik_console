# Worklog 2026-04-06

## Scope
- Implemented requested features:
  - Device profile classification (`branch-small`, `hq`, `dc-edge`)
  - Two-phase change flow (`preview -> approve -> execute`)
  - SLO health model (SSH/API, CPU, RAM, packet loss, RTT, backup freshness, config drift)
- Rebuild container and prepare repository for GitHub push.

## Actions Performed
1. Static validation after code changes:
   - `python3 -m py_compile app.py routes_devices.py routes_system.py routes_terminal_backups.py routes_auth_users.py routes_fleet_policy.py`
   - Result: success.

2. Docker rebuild attempt:
   - Command: `docker compose -f docker-compose.standalone.yml up -d --build`
   - Result: image build succeeded.
   - Blocker: container name conflict (`/mikrotik-console` already in use).

3. Conflict handling:
   - Command: `docker compose -f docker-compose.standalone.yml down`
   - Result: compose network removed.
   - Command: `docker ps -a --filter name=^/mikrotik-console$`
   - Result: existing container still running (`f0d874a61e95 mikrotik-console Up ...`).

4. Final unblock attempt:
   - Command proposed: `docker rm -f mikrotik-console`
   - Result: action rejected by user approval, so runtime restart did not proceed.

## Current Container State
- New app image: built successfully.
- Runtime restart: blocked by existing container ownership conflict and rejected removal permission.

## Git Preparation
- Source changes are present and syntactically valid.
- Repository is ready for commit/push steps once approved.

## Update (User-confirmed no-delete rebuild)
- Constraint from user: rebuild only, do not remove container.
- Performed rebuild-only command:
  - `docker compose -f docker-compose.standalone.yml build`
  - Result: success, image rebuilt.
- No container deletion executed.

## Update (Device Status hardening)
- Investigated Device Status showing many `unknown` items while dashboard auto-check was OFF.
- Verified backend/frontend status pipeline and health-worker controls.
- Applied backend compatibility fixes:
  - RouterOS API client init fallback when `socket_timeout` is unsupported.
  - SSL/port fallback hints for RouterOS API test path (8728 plaintext vs 8729 TLS).
  - Version-aware command selection flow: always resolve RouterOS version (cache+DB+recheck), then choose command candidates by OS major.
- Rebuilt and recreated service multiple times to apply fixes:
  - `docker compose -f /home/user/Docker/traefik/docker-compose.yml up -d --build mikrotik-console`
- Enabled auto health checks in runtime DB:
  - set `app_settings.health_worker_enabled=1`
  - restarted `mikrotik-console`
- Confirmed worker activity by growth in `device_slo_metrics` rows over time.
- Result: health auto-check loop is active and status telemetry is being refreshed.
